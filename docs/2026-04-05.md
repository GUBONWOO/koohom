# 2026-04-05

## 신착순 버그
- **요구:** 새 크롤링 후 새 매물이 맨 위로 안 옴
- **원인:** UPDATE 시 `crawled_at=NOW()` 갱신 → 기존 매물이 더 최신으로 덮어씌워짐
- **수정:** `crawler.js` UPDATE 쿼리에서 `crawled_at=NOW()` 제거

## 타임스탬프 분리
- **요구:** 최초 등록 시각은 고정, 갱신 시각은 따로 기록
- **수정:** `crawled_at` → `created_at`(불변) + `updated_at`(갱신마다 업데이트)
- **파일:** `db.js`, `crawler.js`, `routes/properties.js`
- 기존 데이터 보정: `UPDATE properties SET created_at = crawled_at WHERE created_at IS NULL;`

## 가격 필터 변경
- **요구:** 2천이하 / 3천이하 / 4천이하 / 5천이하 / 5천이상
- **수정:** `client/src/constants.js` PRICE_OPTIONS — 구간 방식 → 이하/이상 방식

## 페이지 딜레이 개선
- **요구:** 1→2페이지 이동 시 딜레이가 심함
- **원인:** COUNT + SELECT 순차 실행, 페이지 이동마다 COUNT 재실행
- **수정:**
  - `routes/properties.js`: `Promise.all` 병렬 실행 + `skipCount` 파라미터 추가
  - `App.jsx`: 페이지 이동 시 COUNT 생략, 필터/정렬 변경 시에만 COUNT 실행

---

# 2026-04-11

## 크롤러 노선 전구간 + 지역 필터
- **요구:** 副都心線·有楽町線·埼京線 전 구간 크롤링 + 지역별 필터 UI 추가
- **수정:** `crawler.js`
  - `area` 단일값 방식 폐기 → `areas: [...]` 배열로 변경
  - `crawlLineArea()` 신설: 지역 1개씩 크롤링 후 item에 `area` 태깅
  - `crawlLineType()`: areas 배열 순회 호출
  - INSERT/UPDATE 쿼리에 `area` 컬럼 추가
  - 노선별 크롤링 지역: 埼京線(saitama·tokyo·kanagawa), 京浜東北線(saitama), 副都心線·有楽町線(tokyo·saitama), 総武線(chiba·tokyo)
- **수정:** `db.js` — `area VARCHAR(50)` 컬럼 + 인덱스 추가
- **수정:** `routes/properties.js` — `area` 쿼리 파라미터 필터 추가
- **수정:** `constants.js` — `AREA_OPTIONS` 추가 (東京都·埼玉県·神奈川県·千葉県)
- **수정:** `FilterBar.jsx`, `App.jsx` — エリア 필터 UI 연결
- 기존 데이터 area 컬럼은 NULL → 다음 크롤링 시 자동 채워짐

## 대표선 기준 필터링 + 노선별 도보 시간 보정
- **요구:** 타겟 노선이 서브로만 포함된 매물, 타겟 노선 기준 도보 시간이 긴 매물 제외
- **문제:**
  - `parseWalkMin`이 transport 내 전체 노선 중 최솟값을 사용 → 타겟 노선이 50분이어도 다른 노선이 5분이면 통과
  - SUUMO URL 검색 결과에 타겟 노선이 2번째로 포함된 매물도 섞여 들어옴
- **수정:** `crawler.js`
  - `parseWalkMinForLine(transport, lineName)` 추가 — 타겟 노선 이름 이후 첫 번째 徒歩XX분만 추출
  - `crawlLineType` 루프에서 3단계 필터:
    1. `transport.startsWith(line.name)` → 대표선이 타겟 노선인 것만 통과
    2. `walk_min`을 타겟 노선 기준으로 재계산
    3. `walk_min <= MAX_WALK_MIN` 재필터

## 역별 필터 추가
- **요구:** 노선 선택 후 역별로 매물 필터링
- **수정:** `db.js` — `station VARCHAR(100)` 컬럼 + 인덱스 추가
- **수정:** `crawler.js`
  - `parseStationForLine(transport, lineName)` 추가 — 노선명 이후 `「역명」` 패턴으로 대표 역명 추출
  - `crawlLineType` 루프에서 `station` 태깅
  - INSERT/UPDATE 쿼리에 `station` 컬럼 추가
- **수정:** `index.js` — `/api/stations?line=xxx` 엔드포인트 추가 (station 컬럼 GROUP BY, 건수 많은 순)
- **수정:** `routes/properties.js` — `station = ?` 필터 추가 (인덱스 활용)
- **수정:** `api.js` — `getStations(line)` 추가
- **수정:** `App.jsx` — 노선 선택 시 역 목록 로드, 역 클릭 시 필터링, 노선 변경 시 역 선택 초기화
- 기존 데이터 station 컬럼은 NULL → 다음 크롤링 시 자동 채워짐

## 総武線 추가
- **요구:** 치바 소부선 추가
- **수정:** `crawler.js` — `総武線 { slug: 'en_sobusen', areas: ['chiba', 'tokyo'] }` 추가
- **수정:** `constants.js` — AREA_OPTIONS에 千葉県 추가

---

# 2026-04-17

## 신축/중고 구분 크롤링 + 필터
- **요구:** 신축(新築) 매물도 크롤링하고 신축/중고 필터 UI 추가
- **수정:** `crawler.js`
  - `URL_TYPES`에 `{ type: 'shinchiku', path: 'ikkodate' }` 추가
  - `property_type: urlType` 버그 수정 → `property_type: urlType.type`
  - INSERT/UPDATE 쿼리에 `property_type` 컬럼 추가
- **수정:** `db.js` — `property_type VARCHAR(20)` 컬럼 + 인덱스 추가
- **수정:** `routes/properties.js` — `propertyType` 필터 추가
- **수정:** `constants.js` — `TYPE_OPTIONS` 추가 (すべて/新築/中古)
- **수정:** `FilterBar.jsx` — 種別 필터 그룹 추가, 新築 선택 시 築年数 비활성화(opacity + pointer-events)
- **수정:** `App.jsx` — typeIdx state + 新築 선택 시 yearIdx 자동 리셋
- **수정:** `App.css` — `.filter-group--disabled` 스타일 추가

## 西武新宿線 · 西武池袋線 추가
- **수정:** `crawler.js` — TARGET_LINES에 추가
  - 西武新宿線 `en_seibushinjukusen` (tokyo·saitama) — 약 1,230건
  - 西武池袋線 `en_seibuikebukurosen` (tokyo·saitama) — 약 1,490건

## stats 쿼리 버그 수정
- **원인:** `/api/stats` 쿼리에서 삭제된 `crawled_at` 컬럼 참조 → 오류로 사이드바 노선 목록 미표시
- **수정:** `index.js` — `MIN/MAX(crawled_at)` 제거

## 지역별 로테이션 크롤링 스케줄
- **요구:** 노선·지역이 늘어나 전체 크롤링 시간이 길어짐 → 지역별로 분산
- **수정:** `index.js` — 단일 크론 → 3개 크론으로 분리
  - 00:00 / 09:00 / 18:00 → tokyo
  - 03:00 / 12:00 / 21:00 → saitama
  - 06:00 / 15:00 → kanagawa + chiba
- **수정:** `crawler.js` — `runCrawler(lineName, targetAreas)` — areas 필터 파라미터 추가

## API 요청 race condition 방지
- **요구:** 필터를 빠르게 변경 시 이전 요청이 늦게 돌아와 최신 결과를 덮어쓰는 문제
- **수정:**
  - `App.jsx`: `abortRef`(useRef) 추가 → 새 요청 시작 전 이전 요청 `abort()`
  - `api.js`: `getProperties(params, signal)` — signal 파라미터 전달 지원
  - abort 시 axios `CanceledError`는 무시 처리
